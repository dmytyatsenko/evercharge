files:
  "/etc/httpd/conf.d/www_redirect.conf":
    mode: 000644
    owner: root
    group: root
    content: |
      RewriteEngine On
      RewriteCond %{HTTP_HOST} ^www.evercharge.net$ [NC]
      RewriteRule (.*) https://evercharge.net%{REQUEST_URI} [R=301,L]
